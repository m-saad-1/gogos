### Refined Prompt

Implement the following refinements across the website while keeping the existing design language and functionality consistent:

1. **Professional Icons**

   * Replace the current emojis on the **Profile, Reservation, About, and Contact** pages with proper professional icons.
   * Use a consistent icon library/style throughout the UI.

2. **Bottom Navigation**

   * On the **About** and **Reservation** pages, replace the **About** icon in the bottom navigation with the **Profile** icon.
   * Add a subtle, soft border line along the bottom navigation bar to improve visibility.
   * Make the bottom navigation bar use a refined **glassmorphism** effect.

3. **Chatbot**

   * Remove the existing shadow from the chatbot icon.
   * Add a subtle, soft border around it similar to the **View Cart** bar.
   * Keep the chatbot visually clean and consistent with the rest of the floating UI.

4. **Homepage Menu**

   * Make the Homepage **Menu section exactly match the Menu page's menu section**.
   * Match the structure, category layout, cards, spacing, images, typography, buttons, and interactions exactly.

5. **Menu Item Details**

   * When clicking a menu item image, open the **item details modal**.
   * Inside the modal:

     * Display the menu image in a **square frame**.
     * Show the complete item details clearly.
   * When clicking the image inside the modal, open it in a larger image viewer/lightbox.

6. **Price Formatting**

   * Remove unnecessary decimal zeros from prices throughout the entire website.
   * For example:

     * `2500,00` → `2500`
     * `500.00` → `500`
   * Apply consistent price formatting across Menu, Offers, Cart, Orders, Checkout, Modals, and all other pages.

7. **About Page**

   * Professionally redesign and structure the **Delivery** and **Takeaway** sections.
   * Improve their cards, icons, typography, spacing, and visual hierarchy while keeping them consistent with the restaurant's branding.

8. **View Cart**

   * Make the **View Cart** button/bar on the Homepage fully functional.
   * Ensure it correctly reflects the current cart item count and total price.
   * Clicking it should navigate/open the correct Cart experience.

9. **Menu Card Images**

   * Make all menu item card images **square**.
   * Maintain consistent dimensions and alignment across all menu grids and screen sizes.

10. **Footer Bottom Area**

* Remove the empty/incorrect background color below the footer.
* Extend the footer's background color into this empty space so the entire bottom area visually belongs to the footer.
* Ensure this works correctly across desktop and mobile screens.

Finally, perform a full visual and functional check across **all pages and screen sizes** to ensure the changes are consistent and do not introduce layout shifts, overflow, broken interactions, or inconsistent styling.
